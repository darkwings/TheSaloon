export default function SettingsPage({ onBack }: { onBack: () => void }) {
  return <div style={{ color: '#e8d8b0', padding: 20 }}>Settings (coming soon) <button onClick={onBack}>Back</button></div>
}
